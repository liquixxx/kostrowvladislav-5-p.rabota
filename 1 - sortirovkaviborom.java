//ISTO4NIK: https://javarush.com/quests/lectures/questharvardcs50.level03.lecture08

#include <cs50.h>
#include <stdio.h>
#include <string.h>

int main() {
    // Получаем строку с числами от пользователя
    string num = get_string("Ваши числа:\n");
    
    // Вычисляем длину строки
    int n = strlen(num), j = 0;
    
    // Внешний цикл: проходим по всем элементам кроме последнего
    for (int i = 0; i < n - 1; i++) {
        // Внутренний цикл: проходим по элементам справа от текущего
        for (j = i + 1; j < n; j++) {
            int min = i; // Предполагаем, что текущий элемент минимальный
            
            // Сравниваем текущий элемент с предполагаемым минимальным
            if (num[j] < num[i]) {
                min = j; // Нашли элемент меньше, запоминаем его позицию
            }
            
            // Если нашли элемент меньше текущего, меняем их местами
            if (min != i) {
                int a = num[i];    // Временная переменная для обмена
                num[i] = num[j];   // Записываем меньший элемент на позицию i
                num[j] = a;        // Записываем больший элемент на позицию j
            }
        }
    }
    
    // Выводим отсортированную строку
    printf("%s", num);
}
